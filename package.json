{
  "name": "weather-app",
  "version": "1.0.0",
  "private": true,
  "description": "Simple weather application that runs without external build tooling.",
  "scripts": {
    "test": "node ./scripts/run-tests.js"
  }
}
